{
  "name": "strong-loop-live-api",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticated streaming proxy from Azure Static Web Apps to the Strong Loop Foundry agent",
  "main": "dist/src/{index.js,functions/*.js}",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "start": "node dist/src/server.js",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.11.0",
    "@azure/identity": "^4.11.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.x",
    "@types/express": "^5.0.3",
    "@types/node": "^22.x",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}